import React from 'react';

const SideBar = () => {
     return (
       <div className="  flex justify-center fixed h-screen">
         Side bar component
       </div>
     );
};

export default SideBar;